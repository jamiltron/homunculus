package com.jamiltron.homunculus.model;

import com.badlogic.gdx.math.Vector2;

public class Mob extends Entity {
  Vector2 vel;
  Vector2 acc;
  
  public Mob(float x, float y, float w, float h) {
    super(x, y, w, h);
    vel = new Vector2(0, 0);
    acc = new Vector2(0, 0);
  }
  
  public void update(Float dt) {
    pos.add(vel.tmp().mul(dt));
    bounds.setX(pos.x);
    bounds.setY(pos.y);
  }
}


